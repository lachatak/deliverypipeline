box: tsechov/oracle-java7-sbt.0.13.0@0.0.4
build:
    steps:
        - script:
            name: docker login --username="" --password="" --email=""
            code: |-
                docker login --username="" --password="" --email=""
        - script:
            name: sbt clean compile
            code: |-
                sbt clean compile dockerBuildAndPush